const GRCtrl = require('../controllers/GameRoom.controller');

const { authenticate } = require('../config/jwt.config');

module.exports = app => {

    // [ USERS ]


    // [ GAMES ]

    // [ CHATROOM ]
}